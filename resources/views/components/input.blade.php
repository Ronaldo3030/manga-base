<div class="flex flex-col">
    <label for="{{ $name }}">{{ $title }}</label>
    <input class="border-2 border-gray-300 rounded-lg p-1" id="{{ $name }}" name="{{ $name }}"
        type="{{ $type }}" value="{{ $value }}">
</div>
